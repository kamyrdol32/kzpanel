{
  "name": "@evpanel/shared",
  "version": "0.1.0",
  "description": "Shared enums and DTO contracts for EvPanel (backend, scraper, frontend).",
  "license": "UNLICENSED",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\""
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
